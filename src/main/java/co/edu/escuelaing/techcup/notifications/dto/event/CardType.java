package co.edu.escuelaing.techcup.notifications.dto.event;

/**
 * Copia local del CardType del Servicio de Partidos (no se comparte la clase entre
 * microservicios a propósito). Debe mantenerse en sincronía por contrato, no por código.
 */
public enum CardType {
    YELLOW,
    RED
}
